#!/bin/sh

RECORDING_DIRECTORY="{{ restreamer_recording_directory }}"
RECORDING_SEGMENT_LENGTH="{{ restreamer_recording_segment_length }}"
SOURCE="{{ restreamer_source }}"
DESTINATION="{{ restreamer_destination }}"
