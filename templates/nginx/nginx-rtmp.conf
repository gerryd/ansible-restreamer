rtmp_auto_push on;

rtmp {
	server {
		listen 127.0.0.1:1935;

		application stream {
			live on;

			hls on;
			hls_path /var/www/hls;
			hls_fragment_naming system;
			hls_sync 100ms;

			drop_idle_publisher 20s;
		}
	}
}
